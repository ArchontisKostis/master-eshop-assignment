package uom.eshop.backend.model;

/**
 * Enum representing the different roles that a user can have in the e-shop application.
 * The roles defined in this enum are STORE and CUSTOMER, which represent a store owner and a customer, respectively.
 */
public enum Role {
    STORE,
    CUSTOMER
}